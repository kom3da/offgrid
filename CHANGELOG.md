# CHANGELOG

版は `MAJOR.MINOR.PATCH`。1つの番号で、カリキュラムと学習用コマンド（`tools/offgrid`）の両方を指す。

- **MAJOR**：課題シートの書式など、ツールとの約束が壊れる変更。取り込んだら、ツールも新しい版に入れ替える
- **MINOR**：内容やツールの機能の追加
- **PATCH**：誤りの修正

学習者は `./scripts/update-curriculum.sh <版>` で取り込む。バイナリは、同じ版のリリースに付く。

## v1.1.0（2026-09-18）

独立したレビューで見つかった不具合の修正と、オフラインの縛りの見直し。**学習用コマンドを入れ替えることを勧める**（ブラウザの画面に、安全のための修正が入っている）。

- `offgrid serve` の画面が、ほかのWebページから操作されないようにした。この画面を立てたまま別のサイトを見ているだけで、コミットやpush、テストの実行が起こる状態だった
- `offgrid serve` から、答えのファイル（`answers/`）を開けないようにした
- 課題が増えたときに「できた」を押しても記録されず、同じ課題が出続ける不具合を直した
- 振り返りの書き込みが、別の行（見出しなど）に入ってしまう不具合を直した
- 読むだけのコマンド（`find`、`status` など）が、その日の振り返りファイルを作らないようにした。セッションの回数が1つずれる原因だった
- `offgrid run` と `offgrid serve` が、同じ進行状態を見るようにした。ターミナルとブラウザを行き来できる
- Stage 3 で、PostgreSQL のユニット（D13〜D15）が案内から漏れていたのを直した。ロードマップどおり、メイン枠の中で続けて案内する
- 「残すもの」に書かれたパス付きのファイル（`lab/main.go` など）も、揃っているかを見るようにした
- Web検索の回数制限をやめた。代わりに、答えのコードが載っているページをAIと同じ扱いにした。ネットワークを実際に切るのは O8・C7・閉域AIデーの回だけにし、オフライン教材の事前準備をステージの前提条件から外した
- Day 0 のインストール手順が、CPUの種類を自動で選ぶようにした（x86_64 でそのまま貼れなかった）
- 最初の6回の時間割が、1日に収まらなくなっていたのを直した（3時間の日に、3.5時間のメインを出していた）。短縮の回は、ウォームアップ・メイン・振り返りだけを案内する
- 卒業制作（C1〜C8）の課題シートで、「このユニットで身につけること」の節がブラウザに出ていなかったのを直した（見出しの名前が違っていた）
- 核となる課題が複数あるユニットで、`**本題**` に通し番号（その1、その2…）を振った。どれが核か分からなくなっていた
- 課題シート11本に残っていた「Web検索は0回」の記述を、新しいルールに合わせた
- 「今日の流れ」を組み立てる場所が3か所に分かれていて食い違っていたのを、1つにまとめた
- 長い英単語やパスが、ターミナルの折り返しで途中から切れていたのを直した

## v1.0.1（2026-09-17）

- 古い版の付け方の記述が、2つのファイルに残っていたのを直した

## v1.0.0（2026-09-17）

最初の版。6ステージ・6トラック・全73ユニットの課題シート、学習用コマンド `offgrid`（ターミナルとブラウザの案内）、ドキュメントサイト。
