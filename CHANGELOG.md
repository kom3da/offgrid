# CHANGELOG

版は、学習テキストとしての内容に付ける。カレンダー形式（`vYYYY.MM`。同じ月の2回目以降は `vYYYY.MM.N`）。

## v2026.10.6

学習用コマンドを Go に一本化。

- `tools/offgrid`（Go、標準ライブラリのみ）に作り直し、Python 版（`scripts/offgrid`）は削除。実装を2つ持たない
- Day 0 でバイナリを入れる手順を[はじめかた](docs/02-getting-started.md)に追加（`~/.local/bin` に1行。以後はオフラインで動く）
- リリースを自動化。版のタグを push すると、Linux と macOS 向けのバイナリがそのリリースに付く。**カリキュラムとツールの版が、いつも同じ番号になる**
- 追従漏れを防ぐ仕掛け：`offgrid selftest`（全73シートを読めるかの検査）と `go test`（書式の検査を含む）を、課題シートを変えたときにCIで走らせる
- `git` の上流が無いときに「pushしていないコミット」を誤検知していたのを修正

## v2026.10.5

ペースの自由化と、セッションを案内するコマンド。

- 週1回の前提をやめ、ペースを選べるものとして明記（週1回で約2〜2年半、週2回で約1年〜1年4か月、週3回で約8〜11か月）。「平日の補助メニュー」を「セッションの間の日（優先順）」に組み替えた
- `scripts/offgrid` を追加。AIもネットワークも使わない学習用コマンド（Python 3 の標準ライブラリだけ。追加のインストール不要）。引数なしで、ダッシュボードとメニュー。`run` はセッションを上から順に案内し、**課題を1問ずつ**出す（できた／詰まった／あとで／手がかり／中断）。ほかに `today`、`status`、`next`、`tick`、`unit`、`find`（オフライン全文検索）、`check`、`stuck`、`timer`、`retro`（振り返りの空欄を1つずつ聞く）、`done`、`end`
- 作業漏れの検出は、`PROGRESS.md` の手書きだけでなく、課題のチェックリスト（`work.md`）、各課題シートの「残すもの」に書かれたファイルの有無、振り返りの未記入欄、コミットとpushの状態から判定する
- 中断しても続きから再開できる（進み具合は、ユニットのディレクトリの `work.md` に入る）
- `prompts/next-session.md` を追加（AIを使ってよい日に、次のセッションの準備を頼む文面）
- 課題シートとドキュメントは、ネットワークを切っても読めること（リポジトリの中のMarkdown）を明記
- Ubuntu 26.04（Python 3.14）で、全サブコマンドの動作を確認済み

## v2026.10.4

- 全73ユニットの「平日に読むもの」に、読む章を追加。章番号と章題は、出版社が公開している目次で確認したもの。翔泳社の4冊（出版社サイトが自動取得を拒否）は話題で示し、「章番号は手元の本の目次で確認する」と添えた
- ドキュメントサイトを、ライトテーマ既定に変更（切り替えはこれまでどおり使える）。トップページの余白を調整
- Ubuntu 26.04 での実機確認をもとに3点修正：PostgreSQL 18 は `EXPLAIN ANALYZE` だけでもブロック数が出る（D14）、`random` に範囲を渡せる（D14）、`ubuntu` イメージに `ps` は入っている（F10の誤った注記を削除）
- 課題シートを書くときの決まりを `CLAUDE.md` に明記（章番号は検証済みのものだけ、解答を書かない、表を使わない、など）

## v2026.10.3

学習テキストとしての全面的な作り直し。

- 全ユニットに課題シートを用意（G1〜G20、T1〜T11、D1〜D15、O1〜O9、C1〜C8 の63本を新規作成。F1〜F10は新しい形式に拡充）。形式を「ねらい／身につけること／キーワード／平日に読むもの／準備／課題／発展／詰まりやすいところ／完了条件の確かめ方／残すもの／次につながるユニット」に統一
- ドキュメントの構成とタイトルを統一。ファイル名を `01`〜`06`（進め方）、`10`〜`15`（トラック）、`20`（資料）、`30`（管理者向け）に整理し、トラックのページは「F：基礎（Stage 0）」の形にそろえた。トラックのページから課題の詳細を課題シートへ移し、同じ見出し構成にした
- 表をやめ、ステージやユニットごとの箇条書きに変更（ロードマップ、トラックの各ページ、コードリーディング課題、教材）
- サイト上では押せないチェックボックスを廃止。ステージの確認項目は、振り返りにコピーして○×を付ける運用に。ユニットのチェック欄は、学習者のものである `PROGRESS.md` に全ユニット分を用意
- 教材とツールの一覧に、出版社や公式サイトのURLと、現行の版を追記
- ドキュメントサイト：専用のトップページを追加。課題シートと `prompts/` をサイト内のページにし、GitHubへ飛ばないようにした。サイドバーを「はじめに／進め方／カリキュラム／資料／管理者向け」にグループ化。テーマを変更
- 「offgrid とは」を、対象者・学び方・全体の構成が分かる内容に書き直し

## v2026.10.2

2回目の独立レビューと、ドキュメントサイトの表示確認を受けた修正。

- 改訂の位置付け：改訂は「学習テキストとしての内容を直すこと」で、学習の進み具合とは切り離した。[改訂の運用](docs/30-revision.md)を、テンプレート管理者向けに書き直し。学習の進み具合に付けるタグ（`noai-日付`）を廃止
- 課題シートのファイル名を `README.md` から `TASKS.md` に変更（G11などの成果物のREADMEと衝突するため）。学習者が自分で作るつなぎのシートは `TASKS.local.md`。学習者が書いてよい場所を明記
- 卒業条件を「必須版が動くこと」に統一（発展は任意）
- 時間割：ウォームアップを「再現ドリル1本＋指摘の直し1件」に、`explain.md` の採点を水曜に、メイン①の最初の30分を読書に使えるように変更。目安回数を Stage 0：13〜17回、Stage 3：16〜22回、全体：約105〜140回に見直し
- ステージ通過の実技：見てよいもの、課題の大きさ、ステージごとの合格の基準を定義
- はじめかた：`init.defaultBranch main`、Homebrewの「Next steps」、`~/.ssh/config` の作り方、`no-ai` プロファイルの作り方（既定からコピー、`chat.disableAIFeatures`）、平日に使うAIの置き場所、`cp -n`、取り込みに失敗したときの戻し方を追記
- 課題シート：F7（`main` を先にpush、`~/.ssh/config`）、F8（`nc`で接続を保つ、`127.0.0.1`を指定、`dig +trace`の代替）、F9（完了条件をHTTP/1.1の範囲に）、F1、F3、F6を修正
- watchdeck：`EventSource`は認証ヘッダを付けられないこと、ディスク使用率は`/proc`から取れないことを、課題として明記
- `scripts/gen-access-log.sh`：上位のIPに、はっきりした差が付くように変更。引数の検証を追加
- `scripts/update-curriculum.sh`：置き換える前にダウンロードの中身を検証。テンプレートから消えた課題シートを報告。元に戻す方法を表示
- デバッグドリルのプロンプトにStage 0用の文面を追加。Stage 0とStage 4のオフライン教材、Stage 4のチェックリストを補完
- ドキュメントサイト：サイドバーの入れ子、表の見出しの縦折り返し、スマホでの図の縮小を修正

## v2026.10.1

「まっさらなマシンから始める学習者」の立場で通して見つかった不足の補完。

- テンプレート化：学習者は `gh repo create --template` で自分のリポジトリを作り、その `main` で進める。改訂は `scripts/update-curriculum.sh <版>` で取り込む
- 環境：全員がUbuntuで学習する方針に統一（WindowsはWSL2、macOSはLima、Linuxはそのまま）。macOS向けの注記を削除
- [はじめかた](docs/02-getting-started.md)を新設：Day 0の準備、F6までの「決まり文句」、初回のウォームアップ。macOS＋Limaの手順は実機で検証済み、WSL2は未検証
- Stage 0の課題シート（`drills/foundation/F01〜F10/README.md`）と、ダミーのアクセスログを作る `scripts/gen-access-log.sh` を追加
- `prompts/` に、週次レビュー、`explain.md` の採点、実技の出題、課題シート生成を追加
- `PROGRESS.md` にユニットごとのチェック欄を追加
- 作者のアカウントに依存していた `scripts/push-to-github.sh` を削除

## v2026.10

独立レビューの指摘を受けた改訂。

- 時間割：メイン枠を2時間から3.5時間に拡大（Stage 1〜2はGo・TS 2.5時間＋PostgreSQL 1時間）。コードリーディングとデバッグは1回おきに交代。ウォームアップに「4回以上前の内容」を追加
- 目安回数：全体を60〜90回から約100〜130回に見直し。テストアウト（完了条件を満たせばスキップ）と、時間がない週の最小メニューを追加
- 詰まったときの手順：15分で打ち切りから、「15分で手順に切り替え、45分で打ち切り」に変更。Web検索の上限をステージごとに段階化
- 平日メニュー：月曜を `[no-ai]` コミットのレビューと `explain.md` の採点に、火・木を読書に変更
- ユニット追加：G19（HTTPクライアントとリトライ）、G20（SSE）、T11（SSEの受信）、O9（負荷試験入門）
- ユニット変更：F3に正規表現・`sed`・`awk`、F7にSSH鍵、G4にポインタ、G7にデバッグ、G14とT4にCORS、G17をpgx中心とプレースホルダに、T7とT8の順序を入れ替え、T9に`useEffect`、O1に`/proc`・定期実行・`tar`、O6をAnsibleに。二分探索木・マージソート・ローカルレジストリなどは発展（任意）に
- 完了条件：「説明できる」を `explain.md` とAI採点で確かめる方式に。曖昧な条件（F8、F9、G6、T2、D3、D15、O1、O3）を具体化。ステージ通過に実技を追加し、チェックリストの漏れを補完
- watchdeck：必須版（agent＋server＋web）と発展に分割。C1〜C8を組み直し、Stage 5でのAIの使い方を明記
- 教材：書籍は「最初に買う2冊」と「ステージ開始時に買う」に分け、ステージ開始時のオフライン教材セットアップを追加
- リポジトリを公開（public）にする前提の注意書きを追加。docsサイト（Astro Starlight）を `site/` に追加
- F10のPostgreSQL 18のマウント先、配分表と時間割の矛盾、リンク漏れなど、初版の点検で見つかった修正を含む

## v2026.09

- 初版。Stage 0〜5、ユニット F/G/T/D/O/C、卒業制作 watchdeck、AIなしデーの運用、改訂の運用を定義
