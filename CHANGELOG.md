# CHANGELOG

版は、学習テキストとしての内容に付ける。カレンダー形式（`vYYYY.MM`。同じ月の2回目以降は `vYYYY.MM.N`）。

## v2026.10.2

2回目の独立レビューと、ドキュメントサイトの表示確認を受けた修正。

- 改訂の位置付け：改訂は「学習テキストとしての内容を直すこと」で、学習の進み具合とは切り離した。[改訂の運用](docs/12-revision.md)を、テンプレート管理者向けに書き直し。学習の進み具合に付けるタグ（`noai-日付`）を廃止
- 課題シートのファイル名を `README.md` から `TASKS.md` に変更（G11などの成果物のREADMEと衝突するため）。学習者が自分で作るつなぎのシートは `TASKS.local.md`。学習者が書いてよい場所を明記
- 卒業条件を「必須版が動くこと」に統一（発展は任意）
- 時間割：ウォームアップを「再現ドリル1本＋指摘の直し1件」に、`explain.md` の採点を水曜に、メイン①の最初の30分を読書に使えるように変更。目安回数を Stage 0：13〜17回、Stage 3：16〜22回、全体：約105〜140回に見直し
- ステージ通過の実技：見てよいもの、課題の大きさ、ステージごとの合格の基準を定義
- はじめかた：`init.defaultBranch main`、Homebrewの「Next steps」、`~/.ssh/config` の作り方、`no-ai` プロファイルの作り方（既定からコピー、`chat.disableAIFeatures`）、平日に使うAIの置き場所、`cp -n`、取り込みに失敗したときの戻し方を追記
- 課題シート：F7（`main` を先にpush、`~/.ssh/config`）、F8（`nc`で接続を保つ、`127.0.0.1`を指定、`dig +trace`の代替）、F9（完了条件をHTTP/1.1の範囲に）、F1、F3、F6を修正
- watchdeck：`EventSource`は認証ヘッダを付けられないこと、ディスク使用率は`/proc`から取れないことを、課題として明記
- `scripts/gen-access-log.sh`：上位のIPに、はっきりした差が付くように変更。引数の検証を追加
- `scripts/update-curriculum.sh`：置き換える前にダウンロードの中身を検証。テンプレートから消えた課題シートを報告。元に戻す方法を表示
- デバッグドリルのプロンプトにStage 0用の文面を追加。Stage 0とStage 4のオフライン教材、Stage 4のチェックリストを補完
- ドキュメントサイト：サイドバーの入れ子、表の見出しの縦折り返し、スマホでの図の縮小を修正

## v2026.10.1

「まっさらなマシンから始める学習者」の立場で通して見つかった不足の補完。

- テンプレート化：学習者は `gh repo create --template` で自分のリポジトリを作り、その `main` で進める。改訂は `scripts/update-curriculum.sh <版>` で取り込む
- 環境：全員がUbuntuで学習する方針に統一（WindowsはWSL2、macOSはLima、Linuxはそのまま）。macOS向けの注記を削除
- [はじめかた](docs/00a-getting-started.md)を新設：Day 0の準備、F6までの「決まり文句」、初回のウォームアップ。macOS＋Limaの手順は実機で検証済み、WSL2は未検証
- Stage 0の課題シート（`drills/foundation/F01〜F10/README.md`）と、ダミーのアクセスログを作る `scripts/gen-access-log.sh` を追加
- `prompts/` に、週次レビュー、`explain.md` の採点、実技の出題、課題シート生成を追加
- `PROGRESS.md` にユニットごとのチェック欄を追加
- 作者のアカウントに依存していた `scripts/push-to-github.sh` を削除

## v2026.10

独立レビューの指摘を受けた改訂。

- 時間割：メイン枠を2時間から3.5時間に拡大（Stage 1〜2はGo・TS 2.5時間＋PostgreSQL 1時間）。コードリーディングとデバッグは1回おきに交代。ウォームアップに「4回以上前の内容」を追加
- 目安回数：全体を60〜90回から約100〜130回に見直し。テストアウト（完了条件を満たせばスキップ）と、時間がない週の最小メニューを追加
- 詰まったときの手順：15分で打ち切りから、「15分で手順に切り替え、45分で打ち切り」に変更。Web検索の上限をステージごとに段階化
- 平日メニュー：月曜を `[no-ai]` コミットのレビューと `explain.md` の採点に、火・木を読書に変更
- ユニット追加：G19（HTTPクライアントとリトライ）、G20（SSE）、T11（SSEの受信）、O9（負荷試験入門）
- ユニット変更：F3に正規表現・`sed`・`awk`、F7にSSH鍵、G4にポインタ、G7にデバッグ、G14とT4にCORS、G17をpgx中心とプレースホルダに、T7とT8の順序を入れ替え、T9に`useEffect`、O1に`/proc`・定期実行・`tar`、O6をAnsibleに。二分探索木・マージソート・ローカルレジストリなどは発展（任意）に
- 完了条件：「説明できる」を `explain.md` とAI採点で確かめる方式に。曖昧な条件（F8、F9、G6、T2、D3、D15、O1、O3）を具体化。ステージ通過に実技を追加し、チェックリストの漏れを補完
- watchdeck：必須版（agent＋server＋web）と発展に分割。C1〜C8を組み直し、Stage 5でのAIの使い方を明記
- 教材：書籍は「最初に買う2冊」と「ステージ開始時に買う」に分け、ステージ開始時のオフライン教材セットアップを追加
- リポジトリを公開（public）にする前提の注意書きを追加。docsサイト（Astro Starlight）を `site/` に追加
- F10のPostgreSQL 18のマウント先、配分表と時間割の矛盾、リンク漏れなど、初版の点検で見つかった修正を含む

## v2026.09

- 初版。Stage 0〜5、ユニット F/G/T/D/O/C、卒業制作 watchdeck、AIなしデーの運用、改訂の運用を定義
