# CHANGELOG

版は `MAJOR.MINOR.PATCH`。1つの番号で、カリキュラムと学習用コマンド（`tools/offgrid`）の両方を指す。

- **MAJOR**：課題シートの書式など、ツールとの約束が壊れる変更。取り込んだら、ツールも新しい版に入れ替える
- **MINOR**：内容やツールの機能の追加
- **PATCH**：誤りの修正

学習者は `./scripts/update-curriculum.sh <版>` で取り込む。バイナリは、同じ版のリリースに付く。

## v1.2.2（2026-09-21）

レビューの頼みかたを、依頼文と手順に書き戻した。**同じ失敗を繰り返さないため。**

- `prompts/review-{tool,consistency,coherence}.md` に「**見つけたら1件ずつファイルに追記してから次へ進む。最後にまとめて書こうとしない**」を入れた。広い依頼を1回で投げて打ち切られ、345,753トークンを使って報告が1件も残らなかったことがある
- `docs/30-revision.md` に「レビューの頼みかた」を足した（範囲を分ける、逐次追記、広く読ませる作業に高い推論設定を使わない、使い捨ての複製を対象にする、出た指摘は1件ずつ原文で確かめる）
- リリースのワークフローが、**公開した資産を落として `sha256sum -c` と `offgrid version` まで確かめる**ようになった（これまでは件数だけで、毎回手で確かめていた）。v1.2.2 でこの段が落ちたため、対象のリポジトリを明示するよう直した（リポジトリの外で `gh` を叩いていた）。**v1.2.2 の資産そのものは手で確かめてあり、問題はない**
- サイトのビルドで毎回出ていた `The collection "i18n" does not exist or is empty` を消した（本物のエラーが埋もれるため）

## v1.2.1（2026-09-21）

**学習用コマンドの修正。** 別ベンダーのAI（Codex、`gpt-6-astra`）にレビューさせた結果で、9件すべてこちらで再現した（[Issue #9](https://github.com/kom3da/offgrid/issues/9)）。Claude の別セッションが2回レビューして直したあとのコードから出てきたもので、うち2件は、その修正自体の取りこぼしだった。

記録が消える・壊れるもの：

- **ターミナルとブラウザが同時に書くと、記録が落ちていた。** 50件の更新のうち残ったのは2件で、エラーは0件。リポジトリごとのロックで「読む → 変える → 書く」を囲み、60件が60件残ることを確かめた
- **読めないファイルを「まだ無い」と扱って上書きしていた。** 権限のエラーで、メモ・進み・セッションの記録が消えた。いまは中止してエラーを返す
- 保存すると、0600 のメモが 0644 になっていた。元の権限を引き継ぐ
- 振り返りの欄の識別子が、1つ書くたびにずれていた。同じ送信が別の欄に入っていたのを止めた

外に届く経路：

- **別のサイトから `GET /end` を開かれるだけで、その日の振り返りファイルができていた**（回数が1回進む）。`/session`・`/unit/`・`/retro` は塞いでいたが `/end` が漏れていた
- **`docs/` や `logs/` に置いたリンクから、リポジトリの外を読み書きできた。** `answers/` にも届いた。リンクをたどってから判定する

言っていることと違ったもの：

- 日本語の名前の実行ファイルを「コミットから外した」と表示しながら、実際には外れていなかった（`git` が名前を引用して出すため）
- `TASKS.local.md` だけのユニットが `selftest` と `status` から黙って消えていた
- `PROGRESS.md` に書けなかったとき、「行がありません」という別の理由を表示していた
## v1.2.0（2026-09-21）

**`AGENTS.md` を置いた。** これまで「学習を奪わない」の決まりは `CLAUDE.md` にしか無く、Claude Code 以外のAIは読まなかった。学習者が課題の途中で別のAIに助けを求めたとき、答えを出されても止めるものが無い状態だった（[Issue #9](https://github.com/kom3da/offgrid/issues/9)）。

- リポジトリの根に `AGENTS.md`（13行）。絶対に破られたくない5つ（学習者のコードを勝手に直さない、`answers/` を読まない、課題を解かない、Go は標準ライブラリ、答えの載ったページを勧めない）を書き写し、残りは `CLAUDE.md` を指す
- `.github/` は学習者が消すので、ベンダー固有の指示ファイルは作らない。根に置く1本で通す
- `scripts/update-curriculum.sh` が `AGENTS.md` も取り込む。`check-docs.py` が、要点が落ちていないかを検査する（`CLAUDE.md` だけ直して `AGENTS.md` が古くなるのを止めるため）

## v1.1.3（2026-09-21）

環境で結果が変わりうる記述を、Ubuntu 26.04 の仮想マシンで通しで確かめた（[Issue #3](https://github.com/kom3da/offgrid/issues/3)）。4件の誤りが見つかり、直した。

- **pagila を、動くタグ `pagila-v3.1.0` に固定した。** 配布リポジトリの新しい版（v4 以降）は pgvector 拡張を使うので、`pg-offgrid`（素の `postgres:18`）では schema が読み込めず、**D トラックの入口で止まる**。`env-facts` で月1回、読み込めることを確かめる
- G1：`go test -race -cover ./...` は `[no test files]` を出さない（パッケージ名だけが出る）。`-cover` を付けない場合との違いを書いた
- D9：100万行の表では、`EXPLAIN` の先頭行が `Gather`（並列）になり、`Parallel Seq Scan` はその下に出る。詰まりやすいところに足した
- D12：ウィンドウ関数の計画に出るのは `Sort` とは限らず、`Incremental Sort` のことがある

確かめて記述どおりだったもの：Go 1.27 での `ServeMux` のメソッド＋ワイルドカード（G12）、`go.mod` の `go` 行によるループ変数の挙動の違い（G15）、`-race requires cgo` と `build-essential` で直ること（G1）、npm で TypeScript・Vite・Vitest・ESLint が入り `npx tsc --noEmit --strict` などが動くこと（T5〜T11）、ESLint が設定ファイルを見つけられないときの文言（T7）、`ss`・`strace`・`systemctl status`・`journalctl -u` の出力（O2・O3）、`pg_restore` を `docker exec -i` の標準入力から流せること（D11）。

## v1.1.2（2026-09-21）

課題シートの「平日に読むもの」に残っていた17箇所の章番号を入れた。翔泳社の4冊は、出版社サイトが目次の自動取得を拒否するため空けてあったが、書籍そのものの目次で確認した（[Issue #2](https://github.com/kom3da/offgrid/issues/2)）。

- 『なっとく！アルゴリズム』（G9・G10）、『SQL ゼロからはじめるデータベース操作』（D1〜D5・D8・D10）、『達人に学ぶDB設計徹底指南書』（D6・D7・D9・D13・C1）、『達人に学ぶSQL徹底指南書』（D8・D10・D12・D14）、『［試して理解］Linuxのしくみ』（C7）
- G9 のキューは『なっとく！アルゴリズム』第6章（幅優先探索）の中にある。D10 のビューとサブクエリは、該当する章が『達人に学ぶSQL徹底指南書』に無いので、『SQL ゼロからはじめるデータベース操作』第5章に振り替えた
- 章番号の出どころに「書籍そのものの目次」を加えた（CLAUDE.md、[教材とツール](docs/20-resources.md)）

## v1.1.1（2026-09-21）

Windows（WSL2）の Day 0 を実機で検証した。**手順の誤りは見つからなかった**ので、[はじめかた](docs/02-getting-started.md)の「実機で検証できていない」の断りを外した。

- Windows 11（WSL 2.5.9）＋ Ubuntu 26.04.1 LTS（x86_64）で、`wsl --install` からセッションを始められるところまで通した。`apt` の3本、`offgrid` の x86_64 の判定、`gh auth login`、`gh repo create --template` からクローン、`rm -rf site .github`、`offgrid status`・`next`・`selftest`、`offgrid serve` を WSL2 に立てて Windows のブラウザから `http://localhost:7777` で開くところまで
- WSL2 の Ubuntu 26.04 でも、`ls`・`sort` は uutils、`grep` は GNU、`/tmp` は tmpfs、bash は 5.3、systemd は既定で有効だった。Lima の VM と前提が変わらない
- Windows の節に、ポート転送の1行を足した（Lima の節にはあって、こちらに無かった）
- **C7 の「時刻をずらしても数秒で戻る」に、WSL2 の場合の調べどころを足した。** WSL2 では NTP を止めるだけでは足りず、ホストの時計に合わせる仕組みが巻き戻しを数秒で打ち消す（進めるほうは打ち消さない）。Lima を前提にした書き方のままだと、この障害で行き止まりになる

## v1.1.0（2026-09-18）

目的の言い直し。**測るのは「AIなしで自分でできるか」であって、ネットワークの有無ではない。** 閉域は目的ではなく、AIが使えない場面の一例に戻した。

- 看板を「AIに頼らずに、自分で書ける・読める・直せる」にした。「ネットワークを切った状態で」の要件を、docs と課題シートから外した
- **O7 は「再現できるビルド」**：リポジトリだけから、別の環境で同じ成果物を作る（`build.sh` と `checksums.txt`）。オフライン文書とローカルレジストリの準備は削除
- **O8 は「引き継ぎ」**：AIに書かせて問題を仕込んだサービスを、答えを見ずに読み、直し、機能を足す。このカリキュラムの動機を直接測る唯一のユニット。生成の文面は `prompts/handover.md`
- **C7 は「障害試験と runbook」**：runbook だけからの再構築（自分以外の手で）と、計画した障害の試験が卒業制作の採点。ネットワークを切っての再構築は廃止
- C5（web）を軽くした。必須は直近1時間のグラフまで。24時間・期間の切り替え・生の点の追加は発展へ
- 「閉域AIデー」を廃止した（弱いAIを使えるかは目的ではない）
- Stage 4 の名前を「運用」にした。目安は 21〜27回、全体は約138〜158回
- ディレクトリ名を変えた：`O07-reproducible-build`、`O08-handover`、`C07-fault-drill`（ユニットIDは変えていない）。**v1.0.0 から取り込んだ人は、古い `O07-offline-prep`・`O08-offline-day`・`C07-offline-rebuild` の `TASKS.md` を消し、自分のメモは新しいディレクトリへ移す。** 残したままだと、`offgrid` が「ディレクトリが2個あります」で止まる
- O2・O3・O4 が使う API を、G17 の平たい形から G18 の `cmd/api`＋`internal/`＋`vendor/` の形に揃えた（G18 が「O7、C6 でそのまま使う」と宣言している形）
- 新しい3本は、公開前に実機で通した指摘で直してある：`docker save` の tar は再ビルドで必ず変わるので、O7 の一致の対象はバイナリと `dist/` に限った。O8 の生成プロンプトは、仕込みの書式が検索語にならないように、時刻の問題が UTC でも出るように直した。C7 の runbook は C6 で書き始め、障害の起こし方は Ubuntu 26.04 の VM で確かめた
- 版を出す前の3本のレビュー（ツール／ファイル間の矛盾／筋の通り方）で直したもの：O9 の課題1が旧 O7 の `~/offline-bundle/` を指したままだった。C5 の冒頭と C1・C8 に「24時間のグラフ」が必須のまま残っていた。D13 の前提を「D12（課題6だけ G17 のあと）」に緩めて Stage 3 の1時間枠に収まるようにした。「間の日」の優先順で「準備」を先頭にした（落とすと次の回が始められない）。F1 に絵文字の打ち方、G1 に `build-essential` を足した。「オフライン保存しておく」は全部「手元に保存しておくと速い（必須ではない）」に。30分の説明の頼み方を `prompts/capstone-defense.md` に足した

学習用コマンド（`offgrid`）の修正。学習者の記録を書く経路にテストを足して見つけたもの。

- `offgrid run` で `q`（中断）を押しても中断されず、その手順が「終わった」として記録されていた。中断されるようにした
- `offgrid serve` のメモ欄で、ユニットを読めなかったときにメモが黙って消えていた。エラーを出すようにした
- `selftest` が、課題シートはあるのに `PROGRESS.md` に欄が無いユニットを名指しで報告するようになった（版を取り込んでユニットが増えたとき、`offgrid done` が理由なく失敗するのを防ぐ）
- 同じユニットのディレクトリが2つあるとき、`selftest` と `status` が黙ってそのユニットを抜いて通っていた（`selftest` は「70本」で成功する）。止めて、両方のディレクトリ名を出すようにした
- ターミナルの `offgrid run` とブラウザを行き来すると、片方の記録（「あとで」「詰まった」「枠の済み」）がもう片方の保存で消えていた。書くたびにファイルから読み直して差分を当てるようにした
- ユニットを完了にした直後のコミットが、次のユニットの名前で付いていた。やっていたユニットの名前にした
- コミットで、ビルドした実行ファイルまで `git add -A` に入っていた。自動で外し、外したことを表示する。テンプレートの `.gitignore` に `*.log` を足した
- 別のサイトのページから開かれただけで、その日の振り返りファイルができて回数がずれる穴を塞いだ。検索が `answers/` の中身を出していたのも止めた
- 確認コマンドは10分で打ち切る。「完了にする」はサーバー側でも完了条件を見る
- テストのカバレッジ 46% → 76.4%。CI は `-race -shuffle=on` で回す

## v1.0.0（2026-09-18）

最初の版。

- 6ステージ・6トラック、全73ユニットの課題シート（答えを書かない問題集）
- 学習用コマンド `offgrid`：セッションを課題1問ずつ案内し（`run`）、ブラウザでも同じ案内ができる（`serve`）。作業の漏れを見つける（`status`）。AIもネットワークも使わない
- ステージごとの確認項目と実技、`explain.md` の採点、再現ドリルによる見直し
- テンプレートとして配り、学習者は自分のリポジトリで進める（`scripts/update-curriculum.sh` で取り込む）
- ブラウザで読めるドキュメントサイト

Windows（WSL2）の Day 0 の手順は、この版では実機で検証できていない（[はじめかた](docs/02-getting-started.md)に明記）。
