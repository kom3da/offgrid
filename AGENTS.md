# AGENTS.md

このリポジトリで作業するAIへの指示。**ここには、どのツールでも必ず守ることだけ**を書く。それ以外の決まり（ステージの範囲、課題シートの書き方、レビューの観点、コミット規約）は `CLAUDE.md` にあるので、作業の前に読むこと。

これは学習用のリポジトリで、価値は「**学習者が自分で書いた**」ことにある。先回りして答えを出すと、その価値が消える。急ぐ作業ではないので、遠回りに見えても学習者の手を止めないほうを選ぶ。

1. **学習者が書いたコードを、頼まれずに書き換えない。** レビューを頼まれたら、指摘とヒントを返す。直したコードを出すのは、明示的に頼まれたときだけ
2. **`answers/` は読まない・引用しない。** デバッグドリルと引き継ぎ課題の答えが入っている
3. **`drills/` の課題シート（`TASKS.md`）と `drills/debug/` の問題を解かない。** 解答例も書かない。ヒントは「どこを疑うか」「どの道具で確かめるか」まで。一段ずつ出し、学習者の反応を待つ
4. **Go の課題は標準ライブラリで書く。** 外部ライブラリを勧めてよいのは G17 の `pgx` だけ
5. **答えのコードが載っているページを勧めない。** 学習中に見てよいのは `man`、`--help`、`go doc`、公式ドキュメント、書籍、自分のメモだけ

学習者がいま何をしているかは `PROGRESS.md` にある。**今のステージより先の知識を前提にしない**（例：Stage 1 のコードに goroutine を使った改善案を出さない）。
