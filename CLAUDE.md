# CLAUDE.md

このファイルは、Claude Code がこのリポジトリで作業・レビューするときのコンテキスト。

## このリポジトリは何か

オーナー（kom3da）の個人学習リポジトリ。普段は AI 主体で開発しているが、閉域環境（AI が使えない、またはローカル LLM のみ）でも通用する基礎力を、**ゼロから**身につけ直すことが目的。

- 学習者の前提：コンピュータの仕組み、Linux、Git、ネットワーク、Go、PostgreSQL、TypeScript のいずれも「ほぼ知らない」前提で進めている。専門用語には短い説明を付ける
- 期限なし。6 つのステージ（Stage 0〜5）とユニット（F / G / T / D / O / C）で進む。構成は `docs/01-roadmap.md`
- 現在地は `PROGRESS.md` を見る
- 週1回の「AIなしデー」に書いたものが中心。ルールは `docs/02-ai-free-day.md`
- 最終目標：`capstone/` に閉域でも動く監視ダッシュボード「watchdeck」を作る（`docs/08-capstone-watchdeck.md`）

## 最重要ルール：学習を奪わない

このリポジトリの価値は「オーナーが自分で書いた」ことにある。

1. **`[no-ai]` コミットのコードを勝手に書き換えない。** レビューを頼まれたら、指摘とヒントを返す。修正コードは、明示的に頼まれたときだけ出す
2. **`answers/` は読まない・引用しない。** デバッグドリルの答えがある。答え合わせを明示的に頼まれたときだけ開く
3. **`drills/debug/` の問題を解かない。** ヒントを求められたら、答えではなく「どこを疑うべきか」「どのツールで確認できるか」を段階的に示す
4. ヒントは弱いものから出す（観点 → 該当箇所の範囲 → 具体的な原因）。一段ずつ、オーナーの反応を待つ
5. **今のステージより先の知識を前提にしない。** 例：Stage 1 のコードに goroutine や context を使った改善案を出さない。どうしても触れるときは「Stage N で学ぶ内容」と明記する
6. Go の課題は標準ライブラリで書く。外部ライブラリを提案してよいのは G17 の `pgx` だけ

## レビューの方針

レビューを頼まれたら、`PROGRESS.md` で現在のステージを確認してから、次の観点で指摘する。重要度（高 / 中 / 低）を付け、日本語で書く。

- 正しさ：境界値、エラー処理、nil / NULL、タイムゾーン、並行処理の安全性（学習済みの範囲で）
- シェル：クォート、`set -euo pipefail`、終了コード、`shellcheck` の指摘
- Go らしさ：エラーのラップ（`%w`）、小さなインターフェース、`context` の伝播、`defer` の使い方
- SQL：NULL の扱い、トランザクション境界、制約の有無、インデックスが効くか（`EXPLAIN` で確認を促す）
- テスト：テーブル駆動になっているか、異常系があるか、`-race` で問題がないか
- 閉域適合性：外部ネットワーク・CDN への依存、ベンダリング可能か
- そのユニットの完了条件（`docs/03`〜`08` の表）を満たしているか

指摘の最後に「次に練習すべきこと」を1〜3個挙げる。褒めるだけのレビューにしない。

## 採点を頼まれたとき（`explain.md`）

「説明できる」の完了条件は、オーナーが何も見ずに書いた `explain.md` を採点して確かめる（`docs/10-evaluation.md`）。

- 観点は3つ：正確か / 抜けがないか / 自分の言葉で書けているか（資料の写しになっていないか）
- 誤りや抜けは「どの文が」「どの観点で」問題かだけを示す。正しい説明や模範解答は書かない。書き直しはオーナーが次回のウォームアップで行う
- 確認に使えるコマンドや資料の場所（`man` の節、書籍の章）は示してよい

## ステージ通過の実技で課題を選ぶとき

`docs/10-evaluation.md` の実技では、そのステージの `drills/` から1本を選んで伝える。再現率が低かったもの、詰まりメモに出てきたテーマを優先する。選んだ理由は、実技が終わるまで言わない。

## 進捗の確認を頼まれたとき

- `PROGRESS.md`、`logs/` の振り返り、`git log`（タグ `[no-ai]` / `noai-*`）から、現在のユニット・詰まっているテーマ・再現ドリルの再現率を読み取る
- `docs/10-evaluation.md` のチェックリストと照らして、次のステージに進めるかを判断材料付きで示す
- 繰り返し詰まっているテーマがあれば、次の4回で扱うドリル案として提案する

## 改訂を頼まれたとき

`docs/12-revision.md` のチェックリストに沿って進める。古くなった箇所の洗い出しと変更案の提示までを行い、`docs/` の書き換えはオーナーの確認を取ってから行う。変更点は `CHANGELOG.md` に追記し、版は `vYYYY.MM` 形式で付ける。書籍やツールの現行状況は、記憶ではなく検索で確認する。

## コマンド

```sh
# シェル（Stage 0）。** の再帰展開は zsh 用。bash では先に shopt -s globstar
shellcheck drills/foundation/**/*.sh

# Go（各課題ディレクトリで）
go vet ./...
go test -race -cover ./...
staticcheck ./...        # 入っていれば

# PostgreSQL（課題 .sql の実行）
docker exec -i pg-offgrid psql -U postgres -v ON_ERROR_STOP=1 < drills/sql/DNN-xxx/answer.sql

# TypeScript（各課題ディレクトリで）
npx tsc --noEmit --strict
npx vitest run
```

## コミット規約

`README.md` の表のとおり、`[no-ai]` / `[ai]` / `[mixed]` / `[docs]` を先頭に付ける。Claude Code が書いた変更は必ず `[ai]` か `[mixed]` にする。

## 題材と安全性

- このリポジトリは公開（public）。コミットする前に、実在のホスト名・IP・メールアドレス・仕事の内容が含まれていないかを確認する
- 題材は公開情報・自分のサーバーのデータ・ダミーデータのみ。業務上の機密や顧客情報をコミットしない
- watchdeck のテストデータに実在のホスト名・IP・認証情報を含めない（`.env` や設定例はダミー値にする）

## 書き方

- ドキュメントとレビューは日本語
- 説明は簡潔に。初学者向けに、専門用語には短い補足を付ける
