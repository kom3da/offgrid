# CLAUDE.md

Claude Code がこのリポジトリで作業・レビューするときのコンテキスト。ここに書くのは**Claude が守る制約**だけ。手順の正本は `docs/` と `prompts/` にあるので、写さずにリンクする（二重管理にすると、片方だけ直って食い違う）。

## このリポジトリは何か

学習カリキュラムのテンプレート（`kom3da/offgrid`）と、そこからコピーして作った学習者それぞれのリポジトリの、両方で使われるファイル。AI が使えない環境（閉域、オフライン、ローカル LLM のみ）でも通用する基礎力を、ゼロから身につけ直すためのカリキュラム。誰に向けたものかは `docs/01-overview.md` の「誰のためのものか」にある。

以下、登場する人は2種類。どちらとして頼まれているかで、してよいことが変わる。

- **学習者**：そのリポジトリで学習している本人。`PROGRESS.md` と `logs/` があるリポジトリで頼まれたら、相手は学習者
- **管理する人**：テンプレート（`kom3da/offgrid`）の内容を保守する人。カリキュラムの改訂は、この人とだけ行う（`docs/30-revision.md`）

- 学習者の前提：コンピュータの仕組み、Linux、Git、ネットワーク、Go、PostgreSQL、TypeScript のいずれも「ほぼ知らない」。専門用語には短い説明を付ける
- 期限なし。6 つのステージ（Stage 0〜5）とユニット（F / G / T / D / O / C、全73本）で進む。構成は `docs/03-roadmap.md`、現在地は `PROGRESS.md`
- 学習の環境は、全員が Ubuntu（WSL2、Lima、Linux）。コマンドの説明は Ubuntu 26.04（bash 5.3）を前提にする。PostgreSQL はコンテナ `pg-offgrid`（postgres:18）
- **Ubuntu 26.04 の `ls`・`sort`・`uniq`・`wc`・`cut`・`head`・`tail`・`cat`・`chmod`・`stat`・`shuf` は、GNU ではなく [uutils](https://uutils.github.io/)（Rust 実装）。** `grep`・`sed`・`awk`・`find`・`cp`・`mv`・`rm` は GNU のまま。カリキュラムで使うオプションは両方で効き、`man` も入っている実装の内容が出る（実機で確認済み）。「GNU の〜」と断定する書き方はせず、`man` で確かめる形に書く
- **Ubuntu 26.04 の `/tmp` は tmpfs（メモリ）。** 大きなファイルを作らせる課題は `/tmp` ではなくホームに置く（実機で確認済み）
- 進め方は「AIなしデー」（1セッション）の積み上げ。週に何回やるかは学習者が決める。ルールは `docs/04-ai-free-day.md`
- 学習者は `offgrid` という学習用コマンド（`tools/offgrid`）で進める。`offgrid run` と `offgrid serve` がセッションを1問ずつ案内し、`offgrid status` が作業漏れを出す。AIもネットワークも使わない
- 最終目標：`capstone/` に監視ダッシュボード「watchdeck」を作り、動かし続けられることを試験で示す（`docs/15-track-c-capstone.md`）

## 最重要ルール：学習を奪わない

このリポジトリの価値は「学習者が自分で書いた」ことにある。

1. **学習者が書いたコードを、頼まれずに書き換えない。** レビューを頼まれたら、指摘とヒントを返す。直したコードを出すのは、明示的に頼まれたときだけ（どのファイルが学習者のものかは、下の「書き換えてよいもの、いけないもの」）
2. **`answers/` は読まない・引用しない。** デバッグドリルの答えがある。答え合わせを明示的に頼まれたときだけ開く
3. **`drills/` の課題シートと `drills/debug/` の問題を解かない。** 課題シートの解答例も書かない。ヒントを求められたら、答えではなく「どこを疑うべきか」「どのツールで確認できるか」を段階的に示す
4. ヒントは弱いものから出す（観点 → 該当箇所の範囲 → 具体的な原因）。一段ずつ、学習者の反応を待つ
5. **今のステージより先の知識を前提にしない。** 例：Stage 1 のコードに goroutine や context を使った改善案を出さない。どうしても触れるときは「Stage N で学ぶ内容」と明記する
6. Go の課題は標準ライブラリで書く。外部ライブラリを提案してよいのは G17 の `pgx` だけ
7. **答えのコードが載っているページを勧めない。** セッション中の学習者が見てよいのは `man`、`--help`、`go doc`、公式ドキュメント、書籍、自分のメモだけ（Stack Overflow やブログは、AI と同じ扱いで禁止）。調べ方は、一次情報のどこを引くかで示す

## 書き換えてよいもの、いけないもの

- **学習者のもの**：`PROGRESS.md`、`logs/`、`answers/`、`capstone/`、`drills/` の中の `TASKS.md` 以外（`work.md`、`notes.md`、`explain.md`、コードなど）。**頼まれていない書き換えをしない。** 特に `work.md` と `logs/` は `offgrid` が読み書きしているので、手で直すのは頼まれたときだけ
- **カリキュラム**：`docs/`、`prompts/`、`templates/`、`scripts/`、`tools/`、課題シート（`TASKS.md`）。テンプレート側でだけ変更する。学習者のリポジトリでは `scripts/update-curriculum.sh` で取り込むだけにし、直接書き換えない
- **`docs/` と課題シートの書き換えは、変更案を示して確認を取ってから行う。** 洗い出しと提案までは、頼まれなくてもよい
- 学習者が `prompts/unit-drill.md` で作るつなぎのシートは `TASKS.local.md`（`TASKS.md` は取り込みで上書きされるので使わない）

## 課題シート（`TASKS.md`）を書くとき

見出しの構成は、`drills/foundation/F03-pipeline/TASKS.md` にそろえる（ねらい／このユニットで身につけること／キーワード／平日に読むもの／準備／課題／発展（任意）／詰まりやすいところ／完了条件の確かめ方／残すもの／次につながるユニット）。

- 書くのは問題だけ。解答、解答例、骨組みのコード、完成した設定ファイルを書かない。準備のためのコマンド（ディレクトリ作成、`go mod init`、データ生成）は書いてよい
- 課題は8〜12問。易しい順に並べ、**本題**（完了条件に直結する問い）を明記する。1問なら `**本題**`、核が複数あるユニットは `**本題（その1）**`〜 と通し番号を振る。番号なしの `**本題**` を2つ以上置かない（どれが核か分からなくなる）
- 「Aの場合とBの場合で結果はどう違うか。なぜか」という観察の問いを多く入れる
- 「詰まりやすいところ」は「症状 → どこを疑うか／何で調べるか」まで。原因や答えは書かない
- 表（Markdown のテーブル）は使わず、箇条書きにする。1本70〜130行が目安
- 書籍の章番号は、**出版社が公開している目次か、書籍そのものの目次で確認したものだけ**を書く。記憶で書かない。確認できない本は、話題で示して「章番号は、手元の本の目次で確認する」と添える（`docs/20-resources.md`）
- 確信のないコマンドやオプションは書かない。「`man xxx` で、〜するオプションを探す」という問いの形にする
- 実在のホスト名・IP・認証情報は書かない。IP は RFC 5737 の範囲、ドメインは `example.com` か `.test`
- トラックのページ（`docs/10`〜`15`）の1行は代表的な目安で、正本はシートの「完了条件の確かめ方」。両方を直す

**書式を変えたら、学習用コマンドが読めるかを必ず確かめる。** 見出しの名前や課題の番号は `tools/offgrid` が解析している。片方だけ直すと、画面から節が消えたり、進捗が記録できなくなる（実際に起きた）。

```sh
cd tools/offgrid && go test ./... && go run . selftest
```

`selftest` が見るのは、frontmatter の title、先頭の「> トラック：…」の行、必須の節、課題の数、`**本題**` の付け方、「残すもの」からファイル名が読めるか、`PROGRESS.md` の欄の数。検査を増やすときは `tools/offgrid/selftest.go` に足す。CI は `drills/**/TASKS.md`・`PROGRESS.md`・`templates/`・`tools/` の変更で、これを回す。

## 学習用コマンド（`tools/offgrid`）を触るとき

学習者のファイルを書き換え、`git` を動かし、`serve` で HTTP を待ち受ける。**学習の記録を壊すと取り返せない**ので、課題のコードより厳しく見る。

- **Go の標準ライブラリだけで書く。** 閉域でビルドできることが要件（`go.mod` に依存を足さない）。画面の HTML・CSS は `//go:embed` で埋め込む。CDN もフォントの取得も入れない
- **ファイルの書き込みは `writeFile`（一時ファイル＋`os.Rename`）を使う。** ターミナルとブラウザを行き来しながら使うので、途中で止まっても中身の切れたファイルを残さない
- **書き込みの失敗を握り潰さない。** `_ =` で捨てると、学習者は「記録できた」と思ったまま先へ進む。CLI はエラーを返し、`serve` は `s.fail` で画面に出す
- **`serve` の書き込み・実行は POST 限定＋トークン＋`Origin`/`Sec-Fetch-Site`/`Host` の検証を通す**（`serve_guard.go`）。学習者がこの画面を立てたまま別のサイトを開くだけで操作されるため。新しい書き込み系のパスを足したら、`postOnly` にも足す
- **`/file/` から `answers/` を開かせない。** 道具が最重要ルールを破ることになる
- 課題シートや `PROGRESS.md` の書式を読む処理を変えたら、`selftest` の検査も足す。書式とツールの食い違いは、機械で止める
- ハンドラを足したら `serve_test.go` にテストを足す（メソッド、トークン、書き込み結果）。穴が空いたことを検出できるテストにする

## レビューの方針

レビューを頼まれたら、`PROGRESS.md` で現在のステージを確認してから、次の観点で指摘する。重要度（高 / 中 / 低）を付け、日本語で書く。

- 正しさ：境界値、エラー処理、nil / NULL、タイムゾーン、並行処理の安全性（学習済みの範囲で）
- シェル：クォート、`set -euo pipefail`、終了コード、`shellcheck` の指摘
- Go らしさ：エラーのラップ（`%w`）、小さなインターフェース、`context` の伝播、`defer` の使い方
- SQL：NULL の扱い、トランザクション境界、制約の有無、インデックスが効くか（`EXPLAIN` で確認を促す）
- テスト：テーブル駆動になっているか、異常系があるか、`-race` で問題がないか
- 再現性：外部ネットワーク・CDN への依存、リポジトリだけから作り直せるか
- そのユニットの完了条件（シートの「完了条件の確かめ方」）を満たしているか

指摘の最後に「次に練習すべきこと」を1〜3個挙げる。褒めるだけのレビューにしない。

## 頼まれごとの入口

手順はそれぞれの正本にある。ここに挙げるのは、**どう頼まれても守る制約**だけ。

| 頼まれごと | 手順の正本 | 守る制約 |
| --- | --- | --- |
| セッションの準備 | `prompts/next-session.md` | 課題を解かない。デバッグドリルの答えは `answers/` に置き、内容をチャットに出さない |
| `explain.md` の採点 | `prompts/grade-explain.md`、`docs/06-evaluation.md` | 観点は3つ（正確か／抜けがないか／自分の言葉で書けているか）。誤りは「どの文が・どの観点で」だけを示し、正しい説明や模範解答は書かない。確認に使える資料の場所（`man` の節、書籍の章）は示してよい |
| ステージ通過の実技の出題 | `prompts/practical-exam.md`、`docs/06-evaluation.md` | その場で合否が出る課題に限る（`explain.md` の採点待ちになる課題は選ばない）。選んだ理由は、実技が終わるまで言わない |
| 前回のレビュー | `prompts/weekly-review.md` | 上の「レビューの方針」と「学習を奪わない」に従う |
| 進捗の確認 | `offgrid status`、`docs/06-evaluation.md` | `PROGRESS.md`・`logs/`・`git log` から読み取り、確認項目と照らして判断材料付きで示す。繰り返し詰まっているテーマがあれば、次の回で扱うドリル案を出す |
| つなぎの課題シート | `prompts/unit-drill.md` | 上の「課題シートを書くとき」に従う。ファイル名は `TASKS.local.md` |
| Issue の作成・更新・クローズ | `docs/30-revision.md` の「Issue と Pull Request の使い分け」 | **本文にはいまの正しい状態を書く。訂正はコメントで積まず、本文を直す**（確かめた結果はコメントに残す）。状態のラベル（`未検証`、`未整備`）は、状態が変わったら外す（閉じるときも）。種類と範囲のラベルは残す。閉じるときは、何をどう確かめたかをコメントに書き、`docs/` の記述が変わるならそちらも直す |
| カリキュラムの改訂 | `docs/30-revision.md`、`prompts/revision.md` | テンプレート側でだけ行い、学習の進み具合とは切り離す。書き換えは確認を取ってから行う。書籍やツールの現行状況は、記憶ではなく検索で確認する。変更点は `CHANGELOG.md` に追記し、版は `MAJOR.MINOR.PATCH`（シートの書式などツールとの約束が壊れる変更は MAJOR） |

学習者のリポジトリで改訂を頼まれたら、テンプレートに Issue で報告するよう案内する。

## コマンド

```sh
# カリキュラムの文書（docs・シート・prompts を触ったら）
python3 .github/check-docs.py

# 学習用コマンド（tools/offgrid を触ったら）
cd tools/offgrid && gofmt -l . && go vet ./... && go test -race ./... && go run . selftest

# シェル（Stage 0）
shopt -s globstar && shellcheck drills/foundation/**/*.sh

# Go（学習者の各課題ディレクトリで）
go vet ./... && go test -race -cover ./...
staticcheck ./...        # 入っていれば

# PostgreSQL（課題の .sql の実行。ファイル名は課題シートを見る）
docker exec -i pg-offgrid psql -U postgres -v ON_ERROR_STOP=1 < drills/sql/D03-aggregate/monthly-sales.sql

# TypeScript（各課題ディレクトリで）
npx tsc --noEmit --strict && npx vitest run
```

## 変更の出しかた

**`main` へ直接 push しない。** ブランチで作業し、Pull Request を出す。**ブランチは必ず `main` から切る**（`git checkout -b <名前> main`）。別の PR のブランチの上に切ると、そちらがマージされた瞬間にこちらも「マージ済み」になり、レビューされていないコミットが `main` に入る（実際に起きた）。PR を出す前に `git log origin/main..HEAD --oneline` で、自分のコミットだけであることを見る。**マージはしない**（管理者の操作）。手順と、版を出す前に通すことは `docs/30-revision.md` の「変更の進めかた」にある。

**Issue と PR の本文には、いまの正しい状態を書く。** 作業を進めて内容が変わったら、コメントを足すのではなく本文を直す（確かめた結果はコメントに残す）。

**Issue と PR は使い分ける。** その場で直せるなら Issue を作らず PR だけにする。Issue にするのは、いま手が付けられないもの（実機や現物が要る）と、外から届いた報告。PR で片付くなら本文に `Fixes #N`、一部だけ進めたなら `Refs #N` と書く。詳しくは `docs/30-revision.md` の「Issue と Pull Request の使い分け」。

PRを出す前に、次を通してから、結果をPRの本文に貼る。

```sh
python3 .github/check-docs.py
cd tools/offgrid && gofmt -l . && go vet ./... && go test -race ./... && go run . selftest
```

ルールを引退させたときは、古い言い回しを `.github/check-docs.py` の `RETIRED` に足す（別のファイルに残るのを、機械で止める）。

## コミット規約

`README.md` のとおり、リポジトリによって使うタグが違う。

- **学習者のリポジトリ**：`[no-ai]` / `[ai]` / `[mixed]` / `[docs]`（AIの関与度）。Claude Code が書いた変更は、必ず `[ai]` か `[mixed]` にする
- **テンプレート**：`[curriculum]` / `[tool]` / `[site]` / `[meta]`（何が変わったか）。テンプレートの変更はほぼすべて AI と一緒に書くので、関与度では分けない。複数にまたがるときは、学習者への影響が大きいほう（`[tool]` > `[curriculum]` > `[site]` > `[meta]`）を選ぶ

## 題材と安全性

- テンプレートは公開（public）。学習者のリポジトリも、公開・非公開にかかわらず、公開される前提で扱う。コミットする前に、実在のホスト名・IP・メールアドレス・仕事の内容が含まれていないかを確認する
- 題材は公開情報・自分のサーバーのデータ・ダミーデータのみ。業務上の機密や顧客情報をコミットしない
- watchdeck のテストデータに実在のホスト名・IP・認証情報を含めない（`.env` や設定例はダミー値にする）

## 書き方

- ドキュメントとレビューは日本語
- 説明は簡潔に。初学者向けに、専門用語には短い補足を付ける
