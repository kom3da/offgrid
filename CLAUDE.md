# CLAUDE.md

このファイルは、Claude Code がこのリポジトリで作業・レビューするときのコンテキスト。

## このリポジトリは何か

学習カリキュラムのテンプレート（`kom3da/offgrid`）と、そこからコピーして作った学習者それぞれのリポジトリの、両方で使われるファイル。以下の「オーナー」は、そのリポジトリで学習している本人を指す。

オーナーは、普段は AI 主体で開発しているが、閉域環境（AI が使えない、またはローカル LLM のみ）でも通用する基礎力を、**ゼロから**身につけ直すために、このリポジトリで学習している。

- 学習者の前提：コンピュータの仕組み、Linux、Git、ネットワーク、Go、PostgreSQL、TypeScript のいずれも「ほぼ知らない」前提で進めている。専門用語には短い説明を付ける
- 期限なし。6 つのステージ（Stage 0〜5）とユニット（F / G / T / D / O / C）で進む。構成は `docs/03-roadmap.md`
- 現在地は `PROGRESS.md` を見る。`offgrid status` が、進捗と作業漏れをまとめて出す（AIを使わないコマンド）
- 学習の環境は、全員がUbuntu（WSL2、Lima、Linux）。コマンドの説明はUbuntu（GNU、bash 5）を前提にする
- `drills/` の各ユニットの `TASKS.md` は課題シート（カリキュラムの一部）。それ以外のファイルが、オーナーの書いたもの。オーナーが `prompts/unit-drill.md` で作るつなぎのシートは `TASKS.local.md`
- カリキュラム（`docs/`、`prompts/`、`templates/`、`scripts/`、`tools/`、課題シート）の変更は、テンプレート側で行う。学習者のリポジトリでは `scripts/update-curriculum.sh` で取り込むだけにし、直接書き換えない
- 「AIなしデー」（1セッション）に書いたものが中心。週に何回やるかは学習者が決める。ルールは `docs/04-ai-free-day.md`
- 最終目標：`capstone/` に閉域でも動く監視ダッシュボード「watchdeck」を作る（`docs/15-track-c-capstone.md`）

## 最重要ルール：学習を奪わない

このリポジトリの価値は「オーナーが自分で書いた」ことにある。

1. **`[no-ai]` コミットのコードを勝手に書き換えない。** レビューを頼まれたら、指摘とヒントを返す。修正コードは、明示的に頼まれたときだけ出す
2. **`answers/` は読まない・引用しない。** デバッグドリルの答えがある。答え合わせを明示的に頼まれたときだけ開く
3. **`drills/` の課題シートと `drills/debug/` の問題を解かない。** 課題シートの解答例も書かない。 ヒントを求められたら、答えではなく「どこを疑うべきか」「どのツールで確認できるか」を段階的に示す
4. ヒントは弱いものから出す（観点 → 該当箇所の範囲 → 具体的な原因）。一段ずつ、オーナーの反応を待つ
5. **今のステージより先の知識を前提にしない。** 例：Stage 1 のコードに goroutine や context を使った改善案を出さない。どうしても触れるときは「Stage N で学ぶ内容」と明記する
6. Go の課題は標準ライブラリで書く。外部ライブラリを提案してよいのは G17 の `pgx` だけ
7. **答えのコードが載っているページを勧めない。** セッション中の学習者が見てよいのは `man`、`--help`、`go doc`、公式ドキュメント、書籍、自分のメモだけ（Stack Overflow やブログは、AIと同じ扱いで禁止）。調べ方を示すときは、一次情報のどこを引くかで示す（`docs/04-ai-free-day.md` のルール）

## 課題シート（`TASKS.md`）を書くとき

見出しの構成は、`drills/foundation/F03-pipeline/TASKS.md` にそろえる（ねらい／このユニットで身につけること／キーワード／平日に読むもの／準備／課題／発展（任意）／詰まりやすいところ／完了条件の確かめ方／残すもの／次につながるユニット）。

- 書くのは問題だけ。解答、解答例、骨組みのコード、完成した設定ファイルを書かない。準備のためのコマンド（ディレクトリ作成、`go mod init`、データ生成）は書いてよい
- 課題は8〜12問。易しい順に並べ、**本題**（完了条件に直結する問い）を明記する。1問なら `**本題**`、核が複数あるユニットは `**本題（その1）**`〜 と通し番号を振る。番号なしの `**本題**` を2つ以上置かない（どれが核か分からなくなる）
- 「Aの場合とBの場合で結果はどう違うか。なぜか」という観察の問いを多く入れる
- 「詰まりやすいところ」は「症状 → どこを疑うか／何で調べるか」まで。原因や答えは書かない
- 表（Markdownのテーブル）は使わず、箇条書きにする。1本70〜130行が目安
- 書籍の章番号は、**出版社が公開している目次で確認したものだけ**を書く。記憶で書かない。確認できない本は、話題で示して「章番号は、手元の本の目次で確認する」と添える（`docs/20-resources.md` を参照）
- 確信のないコマンドやオプションは書かない。「`man xxx` で、〜するオプションを探す」という問いの形にする
- 環境はUbuntu（bash 5、GNUコマンド）。PostgreSQLはコンテナ `pg-offgrid`（postgres:18）
- 実在のホスト名・IP・認証情報は書かない。IPはRFC 5737の範囲、ドメインは `example.com` か `.test`

**課題シートの書式を変えたら、必ず学習用コマンドが読めるかを確かめる。** 見出しの名前や課題の番号の付け方は、`tools/offgrid` が解析している。片方だけ直すと、画面から節が消えたり、進捗が記録できなくなる。

```sh
cd tools/offgrid && go test ./... && go run . selftest
```

`selftest` が検査するのは、frontmatter の title、先頭の「> トラック：…」の行、必須の節、課題の数、`**本題**`（番号なしが2つ以上ないか）、「残すもの」からファイル名が読めるか、`PROGRESS.md` の欄の数。検査を増やしたいときは `tools/offgrid/selftest.go` に足す。CIは `drills/**/TASKS.md`・`PROGRESS.md`・`templates/`・`tools/` の変更で、これを回す。

## レビューの方針

レビューを頼まれたら、`PROGRESS.md` で現在のステージを確認してから、次の観点で指摘する。重要度（高 / 中 / 低）を付け、日本語で書く。

- 正しさ：境界値、エラー処理、nil / NULL、タイムゾーン、並行処理の安全性（学習済みの範囲で）
- シェル：クォート、`set -euo pipefail`、終了コード、`shellcheck` の指摘
- Go らしさ：エラーのラップ（`%w`）、小さなインターフェース、`context` の伝播、`defer` の使い方
- SQL：NULL の扱い、トランザクション境界、制約の有無、インデックスが効くか（`EXPLAIN` で確認を促す）
- テスト：テーブル駆動になっているか、異常系があるか、`-race` で問題がないか
- 閉域適合性：外部ネットワーク・CDN への依存、ベンダリング可能か
- そのユニットの完了条件（`drills/<トラック>/<ユニット>/TASKS.md` の「完了条件の確かめ方」と、`docs/10`〜`15` のトラックのページ）を満たしているか

指摘の最後に「次に練習すべきこと」を1〜3個挙げる。褒めるだけのレビューにしない。

## 次のセッションの準備を頼まれたとき

`prompts/next-session.md` の依頼。`PROGRESS.md` と `logs/` から次のユニットを確認し、そのユニットの `TASKS.md` の「準備」「平日に読むもの」をもとに、ネットワークが要る作業（取得・インストール）と読む章を一覧にする。課題そのものは解かない。デバッグドリルの生成を頼まれたら、答えは `answers/` に置き、内容をチャットに出さない。

## 採点を頼まれたとき（`explain.md`）

「説明できる」の完了条件は、オーナーが何も見ずに書いた `explain.md` を採点して確かめる（`docs/06-evaluation.md`）。

- 観点は3つ：正確か / 抜けがないか / 自分の言葉で書けているか（資料の写しになっていないか）
- 誤りや抜けは「どの文が」「どの観点で」問題かだけを示す。正しい説明や模範解答は書かない。書き直しはオーナーが次回のウォームアップで行う
- 確認に使えるコマンドや資料の場所（`man` の節、書籍の章）は示してよい

## ステージ通過の実技で課題を選ぶとき

`docs/06-evaluation.md` の実技では、そのステージの `drills/` から1本を選んで伝える。**その場で合否が出る課題に限る**（実行して確かめられるもの。`explain.md` の採点待ちになる課題は選ばない）。再現率が低かったもの、詰まりメモに出てきたテーマを優先する。選んだ理由は、実技が終わるまで言わない。

## 進捗の確認を頼まれたとき

- `PROGRESS.md`、`logs/` の振り返り、`git log`（コミットの先頭の `[no-ai]`）から、現在のユニット・詰まっているテーマ・再現ドリルの再現率を読み取る
- `docs/06-evaluation.md` の確認項目と照らして、次のステージに進めるかを判断材料付きで示す
- 繰り返し詰まっているテーマがあれば、次の4回で扱うドリル案として提案する

## 改訂を頼まれたとき

改訂は、学習テキストとしての内容を直す作業で、テンプレートのリポジトリ（`kom3da/offgrid`）でだけ行う。誰かの学習の進み具合（`PROGRESS.md`、`logs/`）とは切り離す。学習者のリポジトリで頼まれたら、テンプレートにIssueで報告するよう案内する。

`docs/30-revision.md` に沿って進める。古くなった箇所の洗い出しと変更案の提示までを行い、書き換えは管理者の確認を取ってから行う。変更点は `CHANGELOG.md` に追記し、版は `MAJOR.MINOR.PATCH` で付ける（課題シートの書式など、ツールとの約束が壊れる変更は MAJOR）。学習の進み具合にはタグを付けない。書籍やツールの現行状況は、記憶ではなく検索で確認する。

## コマンド

```sh
# シェル（Stage 0）
shopt -s globstar && shellcheck drills/foundation/**/*.sh

# Go（各課題ディレクトリで）
go vet ./...
go test -race -cover ./...
staticcheck ./...        # 入っていれば

# PostgreSQL（課題 .sql の実行）
docker exec -i pg-offgrid psql -U postgres -v ON_ERROR_STOP=1 < drills/sql/D03-aggregate/monthly-sales.sql

# TypeScript（各課題ディレクトリで）
npx tsc --noEmit --strict
npx vitest run
```

## コミット規約

`README.md` のとおり、`[no-ai]` / `[ai]` / `[mixed]` / `[docs]` を先頭に付ける。Claude Code が書いた変更は必ず `[ai]` か `[mixed]` にする。

## 題材と安全性

- テンプレートは公開（public）。学習者のリポジトリも、公開・非公開にかかわらず、公開される前提で扱う。コミットする前に、実在のホスト名・IP・メールアドレス・仕事の内容が含まれていないかを確認する
- 題材は公開情報・自分のサーバーのデータ・ダミーデータのみ。業務上の機密や顧客情報をコミットしない
- watchdeck のテストデータに実在のホスト名・IP・認証情報を含めない（`.env` や設定例はダミー値にする）

## 書き方

- ドキュメントとレビューは日本語
- 説明は簡潔に。初学者向けに、専門用語には短い補足を付ける
