---
title: "改訂の運用"
---

このページは、テンプレート（`kom3da/offgrid`）を管理する人のためのもの。学習者は読まなくてよい。

改訂とは、学習テキストとしての内容（`docs/`、課題シート、`prompts/`、`templates/`、`scripts/`）を直すこと。誰かの学習の進み具合とは関係なく、内容上の理由だけで行う。学習者は、好きなときに `scripts/update-curriculum.sh <版>` で取り込む（[はじめかた](02-getting-started.md)）。

## 版の付け方

版は `MAJOR.MINOR.PATCH`（例：`v1.2.0`）。1つの番号で、カリキュラムと学習用コマンド（`tools/offgrid`）の両方を指す。

- **MAJOR**：課題シートの書式、`PROGRESS.md` の書き方、`work.md` の形など、**ツールとの約束が壊れる**変更。学習者は、取り込みと同時にツールも入れ替える必要がある。`CHANGELOG.md` に、その旨をはっきり書く
- **MINOR**：ユニットや機能の追加、文章の書き直し
- **PATCH**：誤り、動かない手順、表記の修正

タグを push すると、リリースにバイナリが付く（`.github/workflows/release.yml`）。**カリキュラムとツールの版は、いつも同じ番号**になる。変更点は `CHANGELOG.md` に残す。学習の進み具合（ステージの通過やセッションの回数）には、タグを付けない。

## 変更の進めかた

このテンプレートの開発は、ほとんど Claude Code が書いている。**書いた本人には自分の穴が見えない**（`offgrid serve` に外部から操作できる穴を作り、それを見落とした実例がある）。そのため、人の目と機械の検査を、`main` に入る前に通す。

1. **ブランチで作業し、Pull Request を出す。** `main` へ直接 push しない。Claude Code はマージしない（マージは管理者の操作）
2. **機械の検査を通す。** PR で自動的に走る
   - `docs` … `.github/check-docs.py`（リンク、引退した言い回し、数の整合）と `shellcheck`
   - `tool` … `gofmt`、`go vet`、`go test -race`、`offgrid selftest`
3. **PRの本文を埋める。** 走らせたコマンドと結果をそのまま貼る（テンプレートがある）。「通ると思う」は書かない
4. **手を動かして確かめたことは、そのまま書く。** 実機・VM・ブラウザで見た結果を貼る

### ルールを引退させたとき

古い言い回しを `.github/check-docs.py` の `RETIRED` に足す。**これをやらないと、別のファイルに古いルールが残る**（課題シート11本に「Web検索は0回」が残っていた）。機械で止める。

### 環境の前提が変わったとき

`env-facts` のワークフローが月1回、カリキュラムが前提にしている事実を確かめる（coreutils が GNU か uutils か、`man` が引けるか、postgres:18 のデータのパス、`apt` に道具があるか）。落ちたら、記述を直すか Issue を立てる。

## 版を出す前に通すこと

1. `python3 .github/check-docs.py`
2. `cd tools/offgrid && gofmt -l . && go vet ./... && go test -race ./... && go run . selftest`
3. **別のセッションに3本のレビューを頼む**（`prompts/review-tool.md`、`prompts/review-consistency.md`、`prompts/review-coherence.md`）。指摘を潰すか、Issue に積む。別のベンダーのAIに頼んでもよい（下の「レビューの頼みかた」）
4. `CHANGELOG.md` に、その版の節を書く（リリースのノートは、ここから作られる）
5. タグを push し、リリースの資産が揃ったことを確かめる（件数はワークフローが検証する）

## レビューの頼みかた

書いた本人には自分の穴が見えない。`prompts/review-*.md` を**別のセッション**に渡す。別のベンダーのAIでもよい（2026-09、Codex に `review-tool.md` を渡したところ、Claude の別セッションが2回見たあとのコードから9件の実バグが出た。うち2件は、その修正自体の取りこぼしだった）。

**依頼のしかたで、結果がまるで変わる。** 同じ道具で、9件出たときと0件のときがあった。

- **範囲を分ける。** 「文書全体の矛盾」のような広い依頼を1回で投げない。docs の層／シートを2〜3本に割る。1回で投げて使用量の上限に当たり、345,753トークンを使って報告ゼロで終わったことがある
- **見つけるたびにファイルへ追記させる。** 最後にまとめて書かせると、打ち切られたときに全部消える（`prompts/review-*.md` に、その指示を入れてある）
- **広く読ませる作業に、高い推論設定を使わない。** 網羅性の勝負であって、深さの勝負ではない。範囲を絞って深く見るときだけ上げる
- **使い捨ての複製を対象にする**（`git worktree add --detach`）。本物のリポジトリを触らせない
- **出た指摘は、1件ずつ原文で確かめてから直す。** 別のAIが言ったから正しい、ということはない。実測で崩れた前提が混ざっていたこともある

## 改訂のきっかけ

| 種類 | きっかけ | 範囲 |
| --- | --- | --- |
| 修正 | 誤り、動かない手順、詰まりやすい箇所の報告があったとき（随時） | 該当箇所だけ |
| 追加 | 未整備の部分を書き足すとき（Stage 1以降の課題シート、各ユニットで読む章、未検証の手順の検証） | 追加する部分だけ |
| 定期点検 | 1年に1回 | 全体（下の「定期点検で確かめること」） |
| 全面改訂 | Goのメジャーな変更、主要ツールの置き換え | 構成から見直す |

報告は、GitHubのIssueで受ける。学習者の `logs/` は個人の記録なので、改訂の材料として読みにいかない。

## Issue と Pull Request の使い分け

**Issue は「まだやらない・やれないこと」、PR は「実際に変えたもの」。**

| 状況 | どちらを使うか |
| --- | --- |
| その場で直せる（いまの作業で終わる） | **PR だけ。Issue は作らない**（作っても、すぐ閉じるだけになる） |
| いま手が付けられない（実機・現物・まとまった時間が無い） | **Issue** |
| 大きくて、何回かに分かれる | **Issue** を置き、PR から参照する |
| 外から届いた誤りの報告・提案・質問 | **Issue**（テンプレートが2つある） |
| 確かめて、記録だけ残したい | **Issue のコメント**。記述が変わるなら、あわせて PR も出す |

Issue を先に作るかどうかで迷ったら、**作らない**ほうを選ぶ。やることが1つ増えるより、変更が1つ出るほうがよい。

### 本文とコメント（Issue も PR も）

**本文には「いまの正しい状態」を書く。訂正はコメントで積まず、本文を直す。** コメントで重ねると、次に読む人が全部読まないと何が正しいのか分からなくなる。

- **本文を直す**：前提が変わった、決めたことが変わった、書いてあることが誤りだった、一部が済んだ、**作業を進めて内容が変わった**
- **コメントに残す**：確かめた結果（何をどう確認したか）、外から届いた報告、レビューのやり取り。**やった作業の記録は消さない**

**PR も同じ。** レビューする人が読むのは本文なので、「残っている」と書いた本文のまま、コメントで「全部やった」と足さない（実際にやってしまった）。作業を進めたら本文を直す。

Issue で一部が済んだときは、本文に「済んだこと」と「残っていること」を分けて書き、確認の詳細はコメントに置いて本文から参照する。

### PR から Issue をつなぐ

- その PR で**片付くなら**、本文に `Fixes #N` と書く（マージで自動的に閉じる）
- **一部だけ進めたなら** `Refs #N` と書く（開いたままにする）。「テストを増やしたが、まだ足りない」はこちら

### どちらに何を付けるか

- **ラベルは Issue だけに付ける。** 種類（`誤り` など）と状態（`未検証`、`未整備`）と範囲（`curriculum` など）
- **PR は題名の先頭のタグで範囲を示す**（`[curriculum]`、`[tool]`、`[site]`、`[meta]`）。ラベルは付けない

## 未整備の部分

一覧は、テンプレートの [Issues](https://github.com/kom3da/offgrid/issues) にある（`未検証` と `未整備` のラベル）。**ここには書かない。** `docs/` は取り込みで学習者に配られるので、管理者のやることリストを全員に渡してしまう。

誤りや提案の受け付けも Issues で行う（テンプレートが2つある：誤りの報告／改訂の提案）。学習者から届いたものは、改訂のきっかけとして扱う。

### ラベルの付け方

ラベルには2つの性質がある。**状態のラベルは、状態が変わったら外す。** 外し忘れると、`未検証` で検索したときに、検証が済んだものが混ざる（実際に起きた）。

- **状態**：`未検証`（書いてあるが実機で確かめていない）、`未整備`（欠けていると分かっている）
  - 確かめた／作ったら**外す**。閉じるときも外す（閉じただけでは、検索に残る）
- **種類**：`誤り`、`提案`、`質問`
  - 何として届いたかの記録なので、**そのまま残す**
- **範囲**：`curriculum`、`tool`、`site`、`meta`（コミットのタグと同じ語）
  - **そのまま残す**

閉じるときは、**何をどう確かめたかをコメントに書く**。検証で分かった事実が `docs/` や `CLAUDE.md` の記述を変えるなら、そちらも直してから閉じる（Issue にだけ書いて終わらせない）。

全73ユニットの課題シートは揃っている。新しいユニットを足したときは、`prompts/unit-drill.md` の文面で書き、`drills/<トラック>/<ユニット>/TASKS.md` に置く。学習者は、テンプレートにまだシートがないユニットに着いたとき、同じ文面でつなぎのシート（`TASKS.local.md`）を自分で作れる。

## 定期点検で確かめること

- Go・Node.js・PostgreSQL・Docker・Ubuntuの現行バージョンを確認し、課題の記述（構文、標準ライブラリ、イメージ名）を合わせる
- 書籍の版が更新されていないか、絶版になっていないかを確認する
- 推奨ツール（静的解析、負荷試験、ローカルLLM、グラフライブラリ、Lima）が今も主流かを確認する
- オフライン化の手順（DevDocs、pkgsite）と、[はじめかた](02-getting-started.md)の手順が今も動くかを、実機で試す
- 卒業制作の要件が、教えているユニットだけで実現できるかを見直す
- 変更点を `CHANGELOG.md` に書き、版のタグを付ける（`MAJOR` を上げるときは、ツールの入れ替えが必要な旨も書く）

## 手順

1. `prompts/revision.md` をClaude Codeに渡し、古くなった箇所を洗い出す。現行の状況は、記憶ではなく検索で確認させる
2. 提示された変更案を確認し、採用するものだけ反映する
3. 上の「変更の進めかた」のとおり、ブランチで直して PR を出す（`main` は保護してあるので、直接 push はできない）
4. `CHANGELOG.md` にその版の節を書く（リリースのノートは、ここから作られる）
5. マージしたあと、「版を出す前に通すこと」を済ませてから、タグを push する

## 改訂で気をつけること

- ユニットのID（F1、G12など）は変えない。足すときは末尾に足す。学習者の記録が、IDで参照しているため
- 課題シートのファイル名（`TASKS.md`）と、学習者のファイルの置き場所の決まりは変えない。取り込みスクリプトが、この決まりで学習者のファイルを守っている
- 完了条件を厳しくする変更は、`CHANGELOG.md` に明記する。取り込んだ学習者が、通過済みのユニットをやり直すかどうかを自分で決められるようにする
